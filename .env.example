## Example environment variables for the Research Assistant
## Copy this file to `.env` and fill in real values locally.
## Do NOT commit your real `.env` to version control.

# Google Gemini / Google Cloud API Key
GOOGLE_API_KEY=your_google_api_key_here

# Optional: Web search API (SerpAPI, Google Custom Search, etc.)
SERPAPI_API_KEY=your_serpapi_key_here

# Vector Database Configuration
CHROMA_PERSIST_DIRECTORY=./chroma_db

# Model Configuration
MODEL_NAME=gemini-2.0-flash
TEMPERATURE=0.7
MAX_TOKENS=2048

# Optional feature flags
#ENABLE_WEB_SEARCH=False
#MAX_WEB_RESULTS=3

# Notes:
# 1) After copying to `.env`, run the app locally; the code reads values from environment variables
#    via `python-dotenv` (already used in `config.py`).
# 2) If this repo previously contained committed secrets, rotate those keys immediately.